services:
    api:
        build:
            context: .
            dockerfile: Dockerfile
            target: development
        ports:
            - '3000:3000'
        volumes:
            - ./apps/api/src:/workspace/apps/api/src
